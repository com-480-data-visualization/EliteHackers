/**
 * V2 — Weekly Pulse Heatmap (stub).
 * Reads: state.taxiTypes, state.dateRange, state.selectedCell
 * Writes: filterBus.update({ selectedCell }) — NOT YET IMPLEMENTED (Milestone 3)
 * TODO: implement hour-of-day × day-of-week heatmap with D3 using weekly_heatmap.json
 */

export function init(container) {
  // Placeholder — implemented in Milestone 3
}

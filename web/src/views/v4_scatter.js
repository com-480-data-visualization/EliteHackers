/**
 * V4 — Trip Anatomy Scatter (stub).
 * Reads: state.taxiTypes, state.dateRange, state.scatterBrush
 * Writes: filterBus.update({ scatterBrush }) — NOT YET IMPLEMENTED (Milestone 3)
 * TODO: render ~50k sampled trips (fare vs. distance) from trip_sample.json; brush to filter
 */

export function init(container) {
  // Placeholder — implemented in Milestone 3
}

/**
 * V3 — NYC Zone Choropleth (stub).
 * Reads: state.taxiTypes, state.dateRange, state.selectedZone
 * Writes: filterBus.update({ selectedZone }) — NOT YET IMPLEMENTED (Milestone 3)
 * TODO: render taxi_zones.topojson colored by zones_volume.json; click a zone to filter
 */

export function init(container) {
  // Placeholder — implemented in Milestone 3
}
